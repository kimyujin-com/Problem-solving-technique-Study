회의 ID: 717 8521 7199
암호: 12345

#include <stdlib.h>
#include <time.h>
srand(time(NULL));

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

#include <limits.h> INT_MIN

큰 따옴표 사용시 \" 사용할것

int is_prime(int num) 
{
	int i;
	for (i = 2; i < num; i++)
		if (num % i == 0)
			return 0;
	return 1;
}

int is_in(char w[], char ch)
{
	int i;
		for (i = 0; w[i] != '\0'; i++) {
			if (ch == w[i])
				return 0;
		}
		return 1;
}